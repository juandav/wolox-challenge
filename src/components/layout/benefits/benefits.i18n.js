import { defineMessages } from "react-intl"

const messages = defineMessages({
  title: {
    id: 'benefits.title',
    defaultMessage: 'Among the benefits we offer are'
  }
})

export default messages